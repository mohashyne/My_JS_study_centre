//Arithmetic operators

const num1 = 100;
const num2 = 60;
let val;

val = num1 + num2;
val = num1 - num2;
val = num1 * num2;
val = num1 / num2;
val = num1 % num2;


//Math Object
val = Math.PI;
val = Math.E;
val = Math.round(2.8);
val = Math.ceil(2.4);
val = Math.floor(2.8);
val = Math.sqrt(64);
val = Math.abs(-3);
val = Math.min(3,4,1,5);
val = Math.min(3,4,1,-5);
val = Math.max(2,5,10,9);
val = Math.pow(8, 2);
val = Math.random();

val = Math.random() * 20; //in a lot of cases you will want a random whole number, and multiply the by the Max number we want. if you use 20, it will give us between 0 and 19

val = Math.random() * 20 + 1; // if you want 1-20 you just have to add 1 to the 20.

val = Math.floor(Math.random()* 20 + 1);







console.log(val);


